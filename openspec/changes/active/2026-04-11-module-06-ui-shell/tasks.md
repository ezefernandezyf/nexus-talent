# Tasks: Module 06 — UI/UX Feature Shells (Analysis & History)

- [x] 1. sdd-explore: extract visual blocks from `docs/assets` and map to components.
- [x] 2. componentization: implement `Hero`, `AnalysisCard`, `HistoryList`, `EmptyState`, `LoadingSkeleton` under `src/components/ui/` and `src/features/*/components/`.
- [x] 3. content refactor: replace hardcoded texts with props and wire to existing hooks.
- [x] 4. page assembly: update `AnalysisPage` and `HistoryPage` to use new components.
- [x] 5. tests: add snapshot and accessibility tests for components and pages.
- [x] 6. run tests and fix issues until green.
- [ ] 7. open PR linking to SDD artifacts.
