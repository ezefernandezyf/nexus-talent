# Tasks: Module 12 - UI Parity / Visual Polish

## Phase 1: Foundation & CSS
- [x] 1.1 Update `src/index.css` with missing color variables and utilities from DESIGN.md.
- [x] 1.2 Add "Deep Space" background scale and "Ghost Borders" utilities.

## Phase 2: Generic UI Components
- [x] 2.1 Refactor/Create `Button` component to include glassmorphism and subtle gradients.
- [x] 2.2 Refactor/Create `Input` component with proper focus states (glow pulse).
- [x] 2.3 Refactor/Create `Card`/`Modal` components to use `surface_variant` 60% + 24px blur.

## Phase 4: Feature Shell Polish
- [x] 3.1 Update UI in `features/analysis` to align with the design system.
- [x] 3.2 Update UI in `features/auth` and `features/history` with proper elevation and states.
- [x] 3.3 Update UI in `features/settings` ensuring consistent typography (Inter/Space Grotesk).

## Phase 5: Quality Assurance
- [x] 4.1 Perform a Lighthouse audit on core screens.
- [x] 4.2 Fix any performance or accessibility issues found (aim for 95+ score).
